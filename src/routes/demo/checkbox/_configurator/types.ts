export interface CheckboxConfigurations {
	checked: boolean;
	ripple: boolean;
	density: number;
	expandedTouchTarget: boolean;
	allowIndeterminated: boolean;
	required: boolean;
	disabled: boolean;
	readonly: boolean;
}
